$('#<%=@comment.user.username.gsub(/ /, "-") rescue "Vigap"%>-comment-upvote').text(" " + <%= @comment.cached_votes_up %>);
$('#<%=@comment.user.username.gsub(/ /, "-") rescue "Vigap"%>-comment-downvote').text(" " + <%= @comment.cached_votes_down %>);
$('.<%=@comment.user.username.gsub(/ /, "-") rescue "Vigap"%>-rank').text(<%=@comment.user.ranking rescue 0%>);
